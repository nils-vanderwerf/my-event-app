<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">My Events App</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
        
        <% if user_signed_in? %>
            <p>Logged in as <%= current_user.name %>
            <li class="nav-item">
                <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'nav-link btn-primary' %>
            </li>
        <% else %>
            
            <li class="nav-item">
                <%= button_to 'Sign In', new_user_session_path, class: 'nav-link btn-primary' %>
            </li>
            <li class="nav-item">
                <%= button_to 'Register', new_user_registration_path, class: 'nav-link btn-primary' %>
            </li>
        <% end %>
          <li class="nav-item">
                <%= button_to 'Create a New Event', new_event_path, class: 'nav-link btn-primary' %>
            </li>
    </ul>
  </div>
</nav>