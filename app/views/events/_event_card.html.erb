
<div class="col-md-4 mb-4">
        <div class="card">
            <div class="card-body">
                <h3 class="card-title"><%= name %></h3>

                <p class="event-date"><%= start_time %> </p>
                <p class="event-date"><%= start_date %></p>
                <%= link_to 'Learn More', event_path(id: id), class: "btn btn-outline-primary btn-block" %>
            </div>
        </div>
     </div>